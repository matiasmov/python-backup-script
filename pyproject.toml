[project]
name = "python-backup-script"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "pyinstaller>=6.19.0",
    "requests>=2.33.1",
]
